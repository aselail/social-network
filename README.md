
# Soical Network

To build docker image

`docker build -t social-network .`
`docker run -p 8080:8080 --rm -it social-network`

For a quick test

`sudo docker build -t social-network . && sudo docker run -p 8080:8080 --rm -it social-network`
